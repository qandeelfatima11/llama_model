services:
  llama_training:
    build: .
    volumes:
      - ./src:/app/src
      - ./src/resources.log:/app/src/resources.log
    container_name: llama_training_container
    tty: true
    stdin_open: true
